<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>BandUp</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!--
	  Required, not optional dressing: iOS terminates the app the moment a
	  feature asks for one of these and the string is absent, and App Review
	  reads the wording. The microphone and speech strings are quoted from
	  APPSTORE.md, which is where the agreed phrasing lives — say what is
	  recorded and what it is used for, and nothing broader than the feature
	  actually does.

	  The camera one guards a feature nobody would file under "camera": the
	  profile picture is a plain file input, and iOS answers one that accepts
	  images with a sheet offering Take Photo or Video beside the photo
	  library. With no string here, that tap is the termination above rather
	  than a refusal — the learner loses the app, not just the photo.
	-->
	<key>NSCameraUsageDescription</key>
	<string>BandUp takes a photo when you choose the camera for your profile picture, so it can show it on your account.</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>BandUp records your answers during a practice speaking test so it can score them.</string>
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>BandUp converts your spoken answers to text so an examiner model can grade your speaking.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!--
	  The redirect Google sends the app back to after a native sign-in.

	  It is the iOS OAuth client ID with its dot-separated parts reversed, which
	  is the form Google requires for an installed application, and iOS will
	  only deliver a URL with this scheme to the app that claims it here.
	  GoogleSignInPlugin.swift builds the same string from the client ID it is
	  given at run time, so the two agree by construction rather than by anyone
	  remembering to keep them in step.

	  The value below is a placeholder. Replacing it is one of the three steps
	  in DEPLOY.md's "Google sign-in in the iOS app", and the button does not
	  appear in the app until the matching GOOGLE_IOS_CLIENT_ID is set on the
	  Worker — so an app built before that step is done offers Apple and email
	  rather than a Google button that would fail.
	-->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.yiuadam.bandup.google-signin</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>com.googleusercontent.apps.YOUR-ID</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
